# Dependências de execução — Documentos Arena
# Instale com: pip install -r requirements.txt

Django>=5.2,<5.3
django-environ>=0.11,<0.13
psycopg[binary]>=3.2,<4.0

# Fila (AGENTS.md §2, D7)
celery[redis]>=5.4,<6.0
redis>=5.0,<7.0

# Armazenamento em S3 (AGENTS.md §5.4, D6)
django-storages[s3]>=1.14,<2.0
boto3>=1.34,<2.0

# Interface
django-htmx>=1.19,<2.0

# Servidor e estáticos
gunicorn>=22.0,<24.0
whitenoise>=6.6,<7.0
